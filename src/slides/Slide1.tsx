import React from "react";

/**
 * Placeholder component for the first slide.
 * @returns {React.ReactElement} The Slide1 component.
 */
export default function Slide1(): React.ReactElement {
  return (
    <div className="flex h-full w-full items-center justify-center bg-blue-100 text-4xl">
      Slide 1 Content
    </div>
  );
}
