/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@/client.ts'
import type { VersionListQueryResponse, VersionListQueryParams } from '../types/VersionList.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/client.ts'

function getVersionListUrl() {
  return `/api/v2/version/` as const
}

/**
 * @description Versions of the games, e.g., Red, Blue or Yellow.
 * @summary List versions
 * {@link /api/v2/version/}
 */
export async function versionList(params?: VersionListQueryParams, config: Partial<RequestConfig> & { client?: typeof client } = {}) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<VersionListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: 'GET',
    url: getVersionListUrl().toString(),
    params,
    ...requestConfig,
  })
  return res
}