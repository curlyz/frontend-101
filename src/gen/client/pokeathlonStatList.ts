/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@/client.ts'
import type { PokeathlonStatListQueryResponse, PokeathlonStatListQueryParams } from '../types/PokeathlonStatList.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/client.ts'

function getPokeathlonStatListUrl() {
  return `/api/v2/pokeathlon-stat/` as const
}

/**
 * @description Pokeathlon Stats are different attributes of a Pokémon's performance in Pokéathlons. In Pokéathlons, competitions happen on different courses; one for each of the different Pokéathlon stats. See [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Pok%C3%A9athlon) for greater detail.
 * @summary List pokeathlon stats
 * {@link /api/v2/pokeathlon-stat/}
 */
export async function pokeathlonStatList(params?: PokeathlonStatListQueryParams, config: Partial<RequestConfig> & { client?: typeof client } = {}) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<PokeathlonStatListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: 'GET',
    url: getPokeathlonStatListUrl().toString(),
    params,
    ...requestConfig,
  })
  return res
}