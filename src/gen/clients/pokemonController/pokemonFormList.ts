/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@/lib/apiClient.ts'
import type { PokemonFormListQueryResponse, PokemonFormListQueryParams } from '../../models/pokemonController/PokemonFormList.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/apiClient.ts'

function getPokemonFormListUrl() {
  return `/api/v2/pokemon-form/` as const
}

/**
 * @description Some Pokémon may appear in one of multiple, visually different forms. These differences are purely cosmetic. For variations within a Pokémon species, which do differ in more than just visuals, the 'Pokémon' entity is used to represent such a variety.
 * @summary List pokemon forms
 * {@link /api/v2/pokemon-form/}
 */
export async function pokemonFormList(params?: PokemonFormListQueryParams, config: Partial<RequestConfig> & { client?: typeof client } = {}) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<PokemonFormListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: 'GET',
    url: getPokemonFormListUrl().toString(),
    params,
    ...requestConfig,
  })
  return res.data
}