/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@kubb/plugin-client/clients/axios'
import type { PokeathlonStatRetrieveQueryResponse, PokeathlonStatRetrievePathParams } from '../../models/pokemonController/PokeathlonStatRetrieve.ts'
import type { RequestConfig, ResponseErrorConfig } from '@kubb/plugin-client/clients/axios'

function getPokeathlonStatRetrieveUrl(id: PokeathlonStatRetrievePathParams['id']) {
  return `https://pokeapi.co//api/v2/pokeathlon-stat/${id}/` as const
}

/**
 * @description Pokeathlon Stats are different attributes of a Pokémon's performance in Pokéathlons. In Pokéathlons, competitions happen on different courses; one for each of the different Pokéathlon stats. See [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Pok%C3%A9athlon) for greater detail.
 * @summary Get pokeathlon stat
 * {@link /api/v2/pokeathlon-stat/:id/}
 */
export async function pokeathlonStatRetrieve(id: PokeathlonStatRetrievePathParams['id'], config: Partial<RequestConfig> & { client?: typeof client } = {}) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<PokeathlonStatRetrieveQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: 'GET',
    url: getPokeathlonStatRetrieveUrl(id).toString(),
    ...requestConfig,
  })
  return res.data
}