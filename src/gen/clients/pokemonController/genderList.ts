/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@/lib/apiClient.ts'
import type { GenderListQueryResponse, GenderListQueryParams } from '../../models/pokemonController/GenderList.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/apiClient.ts'

function getGenderListUrl() {
  return `/api/v2/gender/` as const
}

/**
 * @description Genders were introduced in Generation II for the purposes of breeding Pokémon but can also result in visual differences or even different evolutionary lines. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Gender) for greater detail.
 * @summary List genders
 * {@link /api/v2/gender/}
 */
export async function genderList(params?: GenderListQueryParams, config: Partial<RequestConfig> & { client?: typeof client } = {}) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<GenderListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: 'GET',
    url: getGenderListUrl().toString(),
    params,
    ...requestConfig,
  })
  return res.data
}