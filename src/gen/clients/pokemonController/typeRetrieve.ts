/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@/lib/apiClient.ts'
import type { TypeRetrieveQueryResponse, TypeRetrievePathParams } from '../../models/pokemonController/TypeRetrieve.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/apiClient.ts'

function getTypeRetrieveUrl(id: TypeRetrievePathParams['id']) {
  return `/api/v2/type/${id}/` as const
}

/**
 * @description Types are properties for Pokémon and their moves. Each type has three properties: which types of Pokémon it is super effective against, which types of Pokémon it is not very effective against, and which types of Pokémon it is completely ineffective against.
 * @summary Get types
 * {@link /api/v2/type/:id/}
 */
export async function typeRetrieve(id: TypeRetrievePathParams['id'], config: Partial<RequestConfig> & { client?: typeof client } = {}) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<TypeRetrieveQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: 'GET',
    url: getTypeRetrieveUrl(id).toString(),
    ...requestConfig,
  })
  return res.data
}