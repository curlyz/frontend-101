/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@/lib/apiClient.ts'
import type { StatListQueryResponse, StatListQueryParams } from '../../models/pokemonController/StatList.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/apiClient.ts'

function getStatListUrl() {
  return `/api/v2/stat/` as const
}

/**
 * @description Stats determine certain aspects of battles. Each Pokémon has a value for each stat which grows as they gain levels and can be altered momentarily by effects in battles.
 * @summary List stats
 * {@link /api/v2/stat/}
 */
export async function statList(params?: StatListQueryParams, config: Partial<RequestConfig> & { client?: typeof client } = {}) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<StatListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: 'GET',
    url: getStatListUrl().toString(),
    params,
    ...requestConfig,
  })
  return res.data
}