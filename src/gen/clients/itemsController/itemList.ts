/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import client from '@/lib/apiClient.ts'
import type { ItemListQueryResponse, ItemListQueryParams } from '../../models/itemsController/ItemList.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/apiClient.ts'

function getItemListUrl() {
  return `/api/v2/item/` as const
}

/**
 * @description An item is an object in the games which the player can pick up, keep in their bag, and use in some manner. They have various uses, including healing, powering up, helping catch Pokémon, or to access a new area.
 * @summary List items
 * {@link /api/v2/item/}
 */
export async function itemList(params?: ItemListQueryParams, config: Partial<RequestConfig> & { client?: typeof client } = {}) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<ItemListQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: 'GET',
    url: getItemListUrl().toString(),
    params,
    ...requestConfig,
  })
  return res.data
}